import styled from "styled-components"

export const WrapperItem = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  p {
    margin: 0px;
  }

  label {
    display: flex;
    margin: 5px;
  }
`
